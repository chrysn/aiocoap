the files that look like they are autogenerated actually are, the command used was

    python3.4 =sphinx-apidoc -f -e -o doc iot

they are checked in so automatic documentation building can happen, but might
be removed at any time again and replaced with something that autogenerates
them.
